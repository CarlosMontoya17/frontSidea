import { TurnControlDirective } from './turn-control.directive';

describe('TurnControlDirective', () => {
  it('should create an instance', () => {
    const directive = new TurnControlDirective();
    expect(directive).toBeTruthy();
  });
});
